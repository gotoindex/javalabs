package lr3.models;

/*
 * This small enum class is used instead of a regular boolean value.
 * The reason for this choice is purely for the readability of the code.
 */
public enum Sex {
    MALE,
    FEMALE
}
